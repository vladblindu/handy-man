// colors
const GREEN = 'green'
const RED = 'red'
const BLUE = 'blue'
const YELLOW = 'yellow'
const MAGENTA = 'magenta'

// errors
const RUNERR = 'RUNERR'
const DEVERR = 'DEVERR'

// files
const PKG = 'package.json'
const ENV = '.env'

// Extensions
const PNG_EXT = '.png'
const JSON_EXT = '.json'

module.exports = {
    GREEN,
    RED,
    BLUE,
    YELLOW,
    MAGENTA,
    RUNERR,
    DEVERR,
    PNG_EXT,
    JSON_EXT,
    PKG,
    ENV
}